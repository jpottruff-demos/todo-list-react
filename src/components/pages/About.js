import React from 'react'

export default function   () {
    return (
        // NOTE: this won't actually show in the DOM - but things need to be wrapped in JSX
        <React.Fragment>
            <h1>About</h1>
            <p>This is the todo list app v1.0.0</p>
        </React.Fragment>
    )
}
